{% set productPickerData = nfq_sylius_omnisend_product_picker(settings.product, sylius.localeCode) %}
{% if productPickerData %}
    <script type="text/javascript">
        omnisend.push(
            [
                "track",
                "$productViewed",
                {{ productPickerData|raw }}
            ]
        );
    </script>
{% endif %}
